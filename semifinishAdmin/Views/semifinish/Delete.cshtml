@model semifinishAdmin.Models.siixsem_semifinish_t

@{
    ViewBag.Title = "Delete";
}

<div class="window" style="width:600px;">
    <div class="window-title">BORRAR MODELO</div>
    <div class="window-content">

        <h3>Are you sure you want to delete this?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>Semifinish:</dt>

                <dd>
                    @Html.DisplayFor(model => model.se_description)
                </dd>


                <dt>Linkage Seq:</dt>

                <dd>
                    @Html.DisplayFor(model => model.se_linkage_seq)
                </dd>

            </dl>
            <hr />
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-danger" /> |
                    @Html.ActionLink("Back to List", "Index")
                </div>
            }
        </div>
    </div>
</div>