@model semifinishAdmin.Models.siixsem_cgs_programs_t

@{
    ViewBag.Title = "Delete";
}

<div class="window" style="width:600px;">
    <div class="window-title">BORRAR PROGRAMA COGISCAN</div>
    <div class="window-content">

        <h3>Are you sure you want to delete this?</h3>
        <div>
            <hr />
            <dl class="dl-horizontal">
                <dt>Programa:</dt>

                <dd>
                    @Html.DisplayFor(model => model.se_description)
                </dd>

                <dt>BIN:</dt>

                <dd>
                    @Html.DisplayFor(model => model.se_bin)
                </dd>

            </dl>
            <hr />
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="form-actions no-color">
                    <input type="submit" value="Delete" class="btn btn-default" /> |
                    @Html.ActionLink("Back to List", "Index")
                </div>
            }
        </div>
    </div>
</div>